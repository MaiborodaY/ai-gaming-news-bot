{
  "name": "ai-gaming-news-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI-assisted gaming news Telegram bot on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "syntax": "node --check src/worker.js",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
